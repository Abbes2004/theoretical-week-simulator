# Theoretical Week Simulator - Phase 1 (local, normal simulator)
# Core
pandas>=2.2
numpy>=1.26            # vectorized business-rule engine (src/optimization)
pyarrow>=16.0         # parquet read/write for data/interim and data/canonical
openpyxl>=3.1        # .xlsx reading (table.xlsx, consommation tissu par type.xlsx)
xlrd>=2.0             # legacy .xls reading (cde.xls)

# CLI
click>=8.1

# Web UI (lightweight)
flask>=3.0

# Testing
pytest>=8.0
hypothesis>=6.100      # property-based tests for the business-rule engine

# PDF text extraction (documentation / evidence only, not used in the calculation)
pypdf>=4.0
